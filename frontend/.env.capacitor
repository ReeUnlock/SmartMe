# Native builds (Android/iOS) — must use absolute URL
# No nginx proxy in native WebView, so point directly to production API
VITE_API_URL=https://app.smartme.life/api

# Sentry — set DSN in CI/deploy environment or .env.local
# VITE_SENTRY_DSN=https://your-key@o123.ingest.sentry.io/456
